export interface ButtonProps {
  label?: string;
  // what show up on button

  backgroundColor?: string;
  // bg color

  disabled?: boolean;
  // disable button or not

  onClick?: () => void;
  // when click button
}
