// Props for Text component
export interface TextProps {
  // Text content
  text?: string;

  // Text background color
  backgroundColor?: string;

  // Disable the text or not
  disabled?: boolean;
}
