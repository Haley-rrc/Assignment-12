// Props for TableHeader component
export interface TableHeaderProps {
  // Header text
  text?: string;

  // Header background color
  backgroundColor?: string;

  // Disable the header or not
  disabled?: boolean;
}
