// Props for TableFooter component
export interface TableFooterProps {
  // Footer text
  text?: string;

  // Footer background color
  backgroundColor?: string;

  // Disable the footer or not
  disabled?: boolean;
}
