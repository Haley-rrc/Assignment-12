// Props for HeroImage component
export interface HeroImageProps {
  // Hero title
  title?: string;

  // Hero subtitle
  subtitle?: string;

  // Hero image url
  imageUrl?: string;

  // Hero background color
  backgroundColor?: string;

  // Disable the hero image or not
  disabled?: boolean;
}
