// Props for Label component
export interface LabelProps {
  // Text for the label
  text?: string;

  // Label background color
  backgroundColor?: string;

  // Disable the label or not
  disabled?: boolean;
}
